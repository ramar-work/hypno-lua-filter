#include "books.h"

//Process a web form
int books ( struct HTTPBody *req, struct HTTPBody *res, zTable **t ) {

	//If you could turn the request into a table that'd be great...

	//Then from there, you could probably do other things...
		
	return 1;
}
